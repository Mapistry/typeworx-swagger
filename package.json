{
  "name": "@mapistry/typeworx-swagger",
  "version": "1.0.3",
  "description": "A typeworx plugin library for generating a swagger spec.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "Azad Ratzki",
  "license": "MIT",
  "devDependencies": {
    "@types/debug": "0.0.30",
    "@types/node": "^9.3.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "@mapistry/typeworx": "^1.0.1",
    "debug": "^3.1.0"
  },
  "peerDependencies": {
    "ts-simple-ast": "^11.1.0"
  }
}
